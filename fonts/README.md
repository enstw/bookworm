# Custom Fonts

Place `.ttf`, `.otf`, `.woff`, or `.woff2` font files here and register them in `index.json`:

```json
[
  { "name": "我的字體", "file": "MyFont.woff2" }
]
```

The font will appear in the settings panel font selector.
